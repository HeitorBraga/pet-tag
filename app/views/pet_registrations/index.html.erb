<p id="notice"><%= notice %></p>

<h1 class='title has-text-centered'>Pets</h1>

<table class='mt-6'>
  <thead>
    <tr class='table has-text-centered'>
      <th>Nome</th>
      <th>Tipo</th>
      <th>Raça</th>
      <th>Nascimento</th>
      <th>Dono
      </th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @pet_registrations.each do |pet_registration| %>
      <tr class='table has-text-centered'>
        <td><%= pet_registration.name %></td>
        <td><%= pet_registration.kind %></td>
        <td><%= pet_registration.breed %></td>
        <td><%= pet_registration.birthdate %></td>
        <td><%= pet_registration.person.name %></td>
        <td><%= link_to 'Mostrar', pet_registration, class: "button is-info" %></td>
        <td><%= link_to 'Editar', edit_pet_registration_path(pet_registration), class: "button is-info" %></td>
        <td><%= link_to 'Exculir', pet_registration, method: :delete, data: { confirm: 'Tem certeza?' }, class: "button is-info" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Cadastrar Pet', new_pet_registration_path, class: "button is-info is-fullwidth" %>
